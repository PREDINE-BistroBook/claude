# Zen Recovery — cupping therapy site (Cairo · Dahab · Florence)

A Locali & Ordinazioni build for Ash's friend's team, **Zen Recovery** (Instagram `@zen_recovery10`; brand: italic ZEN, wide-set RECOVERY in electric blue, on black or off-white). Cairo and Dahab are run by Shaarawy (`@recoverywithshaarawy`, bio phone 01145638166, used as the Egypt WhatsApp on the site).. One page: a scroll-driven explanation of what cupping does, then "which city are you in?", then a booking form that takes card payment. **Every booking pays a 2% platform fee to us automatically**, inside Stripe, with no invoicing.

Status (2026-09-12, 14:05 UTC): **LIVE at https://zenrecovery.club** (Worker `zen-recovery`, custom domains zenrecovery.club + www, D1 `zen-recovery`, secrets set, Resend DNS records in place). Deploys run from GitHub Actions on every merge to the default branch touching `sites/zen/`. Prices, addresses and team lines are still placeholders. Photos are real (from Zen's Instagram, supplied by Ash) but which photo belongs to which city is a guess — see `public/img/README.md`. Payments are off until Zen's Stripe account is connected (see below).

```
sites/zen/
  public/index.html     the public site (scroll explainer, cities, rewards, booking)
  public/account.html   client area: rewards + invite link, session history, profile with photo
  public/admin.html     team area: overview + statistics, bookings, clients, services, calendar, team (linked to sign-ins), packs, review, settings (role-scoped)
  public/app.css        shared styles for the two areas
  public/i18n.js        English / Italian / Arabic dictionary + the apply() that translates the page
  public/guide.js       questionnaire options, nearest-room logic, body map, before/after advice, exercises (shared by all pages)
  public/demo.js        API client; answers from sample data when no API is present (preview builds)
  migrations/           schema changes applied to the live database after the first deploy
  public/success.html   "Reserved" page Stripe sends the client back to
  public/img/           real photos — see img/README.md
  src/worker.js         Cloudflare Worker: static site + the whole API (checkout, webhook, accounts, admin)
  src/auth.js           signed cookies, magic-link tokens, PBKDF2 passwords (WebCrypto)
  src/catalog.js        server-side prices (the browser never sets a price)
  schema.sql            D1 tables: users, bookings, credits, admins, settings
  wrangler.toml         deploy config
```

## Accounts, rewards, admin (added 2026-09-12)

**Clients** sign in with a magic link (email, no password). Their account keeps: profile (photo, phone, home city, birthday, notes for the therapist), every session with status and what was paid, rewards, and an invite code.

**Rewards** — both rules are numbers in Settings (owner only), defaults 10 and 40:
- *Loyalty*: when a session is marked **Done** in the admin, the client's completed count goes up; every Nth one issues a "free session" credit. Sessions in any city count. A free session goes through the booking form without a card step.
- *Invite*: every client has a code and a share link (`/account.html?ref=CODE`). A friend who signs up with it gets a 40% credit immediately, usable on their first session. The inviter gets their 40% the moment the friend's first booking is paid (so nobody can farm discounts by inviting themselves). One credit per invited friend.
- Credits are applied at checkout: the Worker recomputes the price server-side and takes the 2% platform fee on the discounted amount. A cancelled booking hands the credit back.
- Sessions added by hand in the admin (cash, walk-in) count toward loyalty if the email matches an account. They carry **no platform fee** — see the note under Ideas.

**Admins** — three kinds, one page (`/admin.html`). Sign-in is an email **or a plain username** (a username gets no booking emails):
- *Amico Mio* (`role = platform`, Ash's account, added 2026-09-12 evening): never receives booking, gift or review emails (`notifyList` excludes the role, and `ZEN_NOTIFY_EMAIL` is now empty). Sees the numbers only — Overview (with the 2% platform fee shown), a **Platform** tab (fee per month and per city, revenue, packs/gifts fees, payments live or off, registered clients, who signs in) and its own profile/password. Every operational endpoint (bookings, clients, services, calendar, team, packs, partners, review, settings, admins) answers 403 to this role. The owner sees the account in "Who can sign in" but can't change or remove it; the role is set straight in D1, never from the UI.
- *Owner* (`role = all`, username `owner` in production): everything the site can do — every city, the reward rules, services and prices, admins. Does **not** see the platform fee anywhere: that's Amico Mio's business, not Zen's.
- *City admin* (`role = florence` etc.): sees only that city's bookings, clients (only people who've had a session there) and money. Cannot see the platform fee, other cities, the rules, or other admins. This is enforced in the Worker (`scope()`), not by hiding buttons.
- Everyone can change their own password. Passwords are PBKDF2-hashed; sessions are signed HttpOnly cookies (12 h for admins, 30 days for clients).
- First owner: set `ADMIN_BOOTSTRAP_EMAIL` and `ADMIN_BOOTSTRAP_PASSWORD` as secrets, sign in once with them, then add the real admins from Settings. The bootstrap only works while the admins table is empty.

**Questionnaire + guide (added 2026-09-12, evening).** On first sign-in a client answers seven short steps: where they live (→ nearest room recommended: Sinai/Red Sea → Dahab, rest of Egypt → Cairo, Italy → Florence, elsewhere → they pick), why they're coming, where it hurts (tap-able body map), activity and experience, health conditions (seven of them are *flags*: pregnancy, blood thinners, bleeding disorders, heart, diabetes, skin, recent surgery), contact preference. Stored in `users.intake` (JSON) + `country`, `city_text`, `nearest_city`. The account then shows a "Your guide" tab (before / after / day after / how often / when to skip, per session type, content in `public/guide.js`), the next session's before-list on the Sessions tab, and the after-list for 48 h after a completed session. The homepage preselects their room (`?city=`) and shows a short before-list under the booking form; the success page shows the checklist. In the admin, every booking and client shows the answers, a read-only body map, and a red **health** flag that must be read before the session. Help boxes on every admin tab explain what the numbers and buttons mean.

**Languages (added 2026-09-12, night).** The public site, the client area and the success page are in English, Italian and Arabic; Arabic flips the layout to right-to-left and uses the "Cairo" typeface. The switcher sits in the nav; the choice is remembered in the browser, saved on the account (`users.lang`), and `?lang=it` in a link forces it. All strings live in `public/i18n.js` (the English text is the key; add a translation and it applies everywhere, including the guide and the questionnaire). The admin stays in English.

**Sign-in with Google.** Server-side OAuth (`/api/auth/google` → Google → `/api/auth/google/callback`). Needs a Google Cloud OAuth "Web application" client: authorised origin `https://zenrecovery.club`, redirect URI `https://zenrecovery.club/api/auth/google/callback`; put the client id in `wrangler.toml` (`GOOGLE_CLIENT_ID`) and the secret in the GitHub secret `GOOGLE_CLIENT_SECRET`. Until both are set the button is hidden and email links are the only way in. Accounts are matched by email, so a client who first used the email link can later use Google.

**More on the client side.** Login bar in the site nav (avatar + first name when signed in, "Sign in" otherwise). Session cards: add to calendar (.ics), "message the room" to reschedule (WhatsApp with a prefilled text; Instagram DM for Florence until it has a number), a 1–5 rating with a word for the therapist once a session is done, and the therapist's note back to the client (written from the admin booking drawer). New **Progress** tab: weekly check-in (pain 0–10, energy, sleep, note), a line of pain over time with session days marked, and simple exercises matched to the focus areas from the questionnaire. Installable as a home-screen app (`manifest.webmanifest`).

**Statistics** (Overview tab): sessions this month with change vs last month, revenue per currency (EUR and EGP are never summed), clients and new clients, upcoming, sessions per month for 12 months, revenue per month per currency, sessions by type, sessions by city (owner, all-cities view), today's list, rewards issued/used. Every chart has a hover tooltip and a table view.

**Preview mode**: without a deployed API, `demo.js` answers from generated sample data so all three areas can be reviewed. The banners at the top say so. Nothing in it runs once `/api` is live.

## How the platform fee works (10% since 2026-09-13, 2% before)

**2026-09-13, Ash:** "The percentage is now 10%." `PLATFORM_FEE_BPS` in `src/catalog.js` went from 200 to 1000; every fee below (Stripe application fee, Fawry recorded fee, statements, join-page terms) follows that one constant. Ash has created the Stripe Connect platform and sent Zen's owner the onboarding link; the site goes live-money once that account exists and `STRIPE_SECRET_KEY` + `ZEN_STRIPE_ACCOUNT` are set (see "Going live").

The original 2% design, still accurate apart from the number:

Stripe Connect, **direct charges with an application fee**:

1. Our Stripe account (`AmicoMioFlorence`, `acct_1U0OjLFtLAIrKWhD`) becomes a **Connect platform**. One-time switch in the Stripe Dashboard: Connect → Get started → "Platform or marketplace". No code.
2. Zen gets a **connected account** under our platform. We send them a Stripe onboarding link; they fill in their business details and bank account (10 minutes, done once). Zen sees their own dashboard, payouts, refunds. We never hold their money.
3. When a client books, the Worker creates a Checkout Session **on Zen's account** (`Stripe-Account` header) with `application_fee_amount = round(price × fee)` (10% now). Stripe splits the payment at settlement: 90% (minus Stripe's processing fee) to Zen, **10% to us**, automatically, on every single booking.
4. The fee shows up in our Dashboard under Connect → Collected fees. Refund a booking and the fee is refunded proportionally (Stripe handles this).

Why this rather than the alternatives:

| Option | Why not |
|---|---|
| Money comes to us, we pay Zen monthly | We'd be merchant of record for a therapy service in two countries: tax, liability, disputes all ours. |
| Zen pays us a monthly invoice for 2% | Depends on Zen's honesty and our bookkeeping. The Connect fee is enforced by Stripe. |
| Deposit only (say 20%) online, rest in cash | Fine if Zen prefers. The fee can still be **2% of the full price** taken from the deposit charge (Stripe only requires fee < charge). Just change `catalog.js`. |

### The Egypt question (needs Ash + Zen)

Stripe **does not open accounts for businesses based in Egypt**. Options, best first:

1. **One connected account, Zen's Italian entity (Florence).** All three cities bill through it, in EUR or EGP. Simplest, one fee stream. Zen must be comfortable declaring Cairo/Dahab income through the Italian business.
2. **Egypt cities take "request to book" instead of payment.** Cairo/Dahab bookings are logged and paid in cash on site; we'd take no 2% there (or a small fixed booking fee charged by card in EUR).
3. **Egyptian payment provider (Paymob/Fawry) for the two Egyptian rooms.** Real work, separate integration, and our fee would have to be invoiced, not automatic.

The site as built supports option 1 out of the box (prices in EGP for Cairo/Dahab; Stripe converts to EUR at payout with a small FX fee), and option 2 with a small change to the form.

## Going live: checklist

Ash's side (Stripe dashboard, one-time):
- [ ] Enable Connect on AmicoMioFlorence, platform type "marketplace".
- [x] Create Zen's connected account — done: `acct_1UFIhGFxTkRNsABQ` (Standard, Italy), in `wrangler.toml` since 2026-09-14. **Onboarding not finished as of 2026-09-14** (no details, no bank account): Zen's owner signs in at dashboard.stripe.com and completes it.
- [ ] Add a **Connect webhook** (Dashboard → Developers → Webhooks → "Listen to events on connected accounts") for `checkout.session.completed` and `checkout.session.expired`, URL `https://<site>/api/stripe-webhook`. Copy the signing secret.
- [ ] Set the Platform Pricing / fee defaults if wanted (the Worker sets the fee explicitly, so optional).

Zen's side:
- [ ] Finish Stripe onboarding (ID + bank account).
- [ ] Confirm prices, durations, which methods each room offers (Hijama is currently listed only for Cairo; Manual therapy is listed everywhere because both Instagram bios lead with it).
- [ ] Florence WhatsApp number + the Florence therapist's name (Egypt uses Shaarawy's public number).
- [ ] Confirm addresses / meeting points, opening hours per room, team lines.
- [ ] Send photos (three portrait shots) + Instagram handle + WhatsApp number.
- [ ] Domain: **zenrecovery.com** (Ash, 2026-09-12: buying it). See "Domain" below.

Deploy — runs on **GitHub Actions** (`.github/workflows/deploy-zen.yml`), because the Claude web sessions in this repo cannot reach `api.cloudflare.com` (org egress policy). Already done from a Claude session on 2026-09-12: the D1 database `zen-recovery` (`35c5461a-9065-4552-a6bf-bac39d1ec8f8`, WEUR) with the schema applied, the Resend domain, `wrangler.toml` pointing at zenrecovery.club.

One-time, Ash, in GitHub → repo → Settings → Secrets and variables → Actions → New repository secret:

| Secret | Value |
|---|---|
| `CLOUDFLARE_API_TOKEN` | a **fresh** token (the one pasted in chat on 2026-09-12 is exposed — roll it). Permissions: Workers Scripts:Edit, D1:Edit, Workers Routes:Edit, Zone:Read, DNS:Edit; zone = zenrecovery.club |
| `CLOUDFLARE_ACCOUNT_ID` | right-hand side of Workers & Pages overview |
| `RESEND_API_KEY` | Resend → API keys → Create → "zen-recovery", Sending access |
| `ADMIN_BOOTSTRAP_PASSWORD` | the first owner password (email is fetta.amore.business@gmail.com); change it in the admin after first login |
| `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET` | later, when Stripe Connect is set up |
| `GOOGLE_CLIENT_SECRET` | optional, with `GOOGLE_CLIENT_ID` in `wrangler.toml`, for "Continue with Google" |

Then Actions → "Deploy Zen Recovery" → Run workflow (or ask a Claude session to trigger it; it can). The workflow: applies the schema, deploys, sets the Worker secrets, generates `SESSION_SECRET` once, and — as soon as zenrecovery.club is a zone in the Cloudflare account — attaches the custom domain and adds the four Resend DNS records itself. Before the zone exists it deploys to `zen-recovery.fetta-amore-business.workers.dev` and says so. Pushes to the repo's default branch touching `sites/zen/` redeploy automatically. GitHub only registers the workflow once the file is on the default branch, so the Zen branch has to be merged before the first run.

Local run: `npx wrangler dev` with `DEV_MAGIC_LINK = "1"` returns the sign-in link in the API response, so accounts can be tested without email.
The preview banner on the public page is already off (`PREVIEW = false`); the account/admin pages show a banner only while no API answers.

Test before real money: create Zen's connected account in **test mode** first, deploy with test keys, book with card `4242 4242 4242 4242`, and check that Connect → Collected fees shows 2% of the amount.

## Domain: zenrecovery.club (bought by Ash on GoDaddy, 2026-09-12)

Workers custom domains need the zone in the same Cloudflare account as the Worker:

1. Cloudflare Dashboard → **Add a site** → `zenrecovery.club` → Free plan → it shows two nameservers (`xxx.ns.cloudflare.com`).
2. GoDaddy → My products → zenrecovery.club → **DNS** → Nameservers → Change → "I'll use my own nameservers" → paste the two → Save. Takes minutes to a few hours. Ignore GoDaddy's warnings about losing its own features.
3. In Cloudflare DNS for the zone, add the **Resend records** below (email from `booking@zenrecovery.club`). Then Resend → Domains → zenrecovery.club → Verify.
4. `deploy.sh` (see Deploy) — the `routes` block in `wrangler.toml` creates the DNS records and certificate for the site itself.

Resend domain already created (region eu-west-1, id `d484efd0-30e7-4633-a220-dfaeec6327df`). Records to add in Cloudflare DNS (all "DNS only", grey cloud):

| Type | Name | Value | Extra |
|---|---|---|---|
| TXT | `resend._domainkey` | `p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQDdmP/M+5vUYIsIjAbtJYbTKGONyOaz+PoIL8HKZMXdmFBUzmC5mwqI1VgkdWHEgtgTncephAlSAttrEiA4k56p5WC4/ufvlfuttRaSYoa8Ul4H+2R2BhaSiYi7FeFCW1nBk19prver1sHuPH3LPLBikAGtcqrNnCJcMXYfvC5nUQIDAQAB` | |
| MX | `send` | `feedback-smtp.eu-west-1.amazonses.com` | priority 10 |
| TXT | `send` | `v=spf1 include:amazonses.com ~all` | |
| CNAME | `rsend` | `send.forge.rmta.net` | |

Keep the domain in Ash's GoDaddy/Cloudflare accounts, not Zen's: it's the one piece of the deal that keeps the site (and the 2%) under Locali & Ordinazioni's control.

## Things to know about the build

- **D1 is used** (accounts, bookings, rewards, admins, calendar, packs, gifts, partners, photos). Ash upgraded the Cloudflare account to the paid Workers plan on 2026-09-12, so the old free-tier daily read cap that the demo sites used to hit is gone; nothing to watch here.
- **Services and prices live in the database** (`services` table), edited in the admin under **Services & prices**: add, rename, reprice, change duration, cover picture (uploaded, resized in the browser; a drawn glyph per treatment type is the fallback), hide/show, remove. The website reads them on load (`GET /api/catalog`) and checkout charges what the table says, never what the browser sends. `src/catalog.js` is only the seed and the fallback if the table is empty. Removing a service that was ever booked hides it instead, so history stays intact. Per-city address, team line, WhatsApp, Maps and review links are in Settings → Per city.
- **Admin profiles**: every admin has a photo, name, phone and an "email me about new bookings" switch (owner: all cities; city admin: their city) under Settings → My profile. The owner can change any admin's role or reset their password from the same page.
- **Emails go through Resend** from `booking@zenrecovery.club` (domain verified 2026-09-12). Booking notifications, gift and pack sales and review requests go to `ZEN_NOTIFY_EMAIL` (Ash's address for now; change it in `wrangler.toml` when Zen has its own). Clients who sign up with Google or Apple get a welcome email, since no magic link was sent to them.
- **Availability: two modes.** Until the admin adds opening hours for a city (Calendar tab), the client picks a day and a morning/afternoon/evening window and Zen confirms the hour on WhatsApp. Once hours exist, the site shows exact free times, per therapist if the team is set up, and a time disappears the moment it's booked (`slotsFor` in `src/lib.js`, checked again server-side at checkout).
- **Motion**: one scroll-scrubbed illustration (GSAP ScrollTrigger) on the home page, and one per method on `method.html?m=dry|sliding|fire|hijama|manual|facial` (scenes, copy and timelines in `public/methods.js`; the method cards and each service card link there, and the page's city buttons come back to the booking form with `?svc=` preselecting the matching service). A breathing ring in the hero, soft reveals. Everything respects `prefers-reduced-motion`.
- **Logo** (supplied by Ash 2026-09-12): `public/img/logo-light.png` (dark wordmark for light headers), `logo-dark.png` (white wordmark for dark backgrounds, footer, method pages), `logo-round.png` (the original round mark), `icon-192/512.png` + `apple-touch-icon.png` + `favicon.png`/`favicon.ico` (app icons), `logo-email.png` (header of every email), `logo-social.png` (1200×630 share preview, `og:image`). Emails now go out as plain text plus an HTML version with the logo on top (`emailHtml` in lib.js).
- **Photos**: real ones are in `public/img/`. Replace any file with the same name and the page picks it up; if a city photo is missing the page falls back to a drawn skyline.
- **Hourly cron** (`src/cron.js`, `[triggers]` in `wrangler.toml`): reminders the day before, "how do you feel?" two days after, birthday credits, waitlist alerts. WhatsApp templates when configured, email otherwise; every send is logged in `messages` and shown in the client's admin drawer.
- **Pages** (Cloudflare serves `booking.html` as `/booking`, and redirects the `.html` form to the clean URL): `/` home (hero, how it works, methods, the room, links to the rest), `/booking` (cities + booking form), `/giftcard`, `/rewards`, `/guide` (before and after), `/method?m=…` (one explainer per method), `/account`, `/admin`, `/export`, `/success`. Shared styles in `public/site.css`; shared script `public/site.js` (config, helpers, status/catalog/me fetches, nav, dialog) with `home.js`, `booking.js`, `gift.js` per page. Links between pages are written as `booking.html` etc. so the preview artifact works too.
- **Hardening (2026-09-12 audit)**: photos (client, admin, therapist, service, before/after) must be a clean base64 image or they're dropped, so nothing can break out of an `<img>` in the admin; every name the pages render is escaped; sign-in links are capped at 3 per account per 15 minutes; today's free slots and "that day has passed" use the city's own time zone, not UTC; service cover pictures are served as cacheable images (`/api/service-photo/:id`) instead of inline in the catalog; approving a health-flagged booking rewards the inviter like any first booking; same-day booking is allowed (past times are filtered); `404.html`, `robots.txt` (admin/account/api excluded) and `sitemap.xml` exist.
- **Code layout**: `src/worker.js` (routing, auth, checkout, webhook, core admin), `src/features.js` (everything added on 2026-09-12: slots, team, packs, gifts, partners, waitlist, export, photos, Apple, review queue, leaderboard), `src/lib.js` (shared helpers, settings, email/WhatsApp/SMS, Stripe session builder, slot maths), `src/cron.js`. An offline API test lives in the session scratchpad (`smoke.mjs`, runs the Worker against an in-memory SQLite copy of `schema.sql`) and a Playwright test of the three pages in demo mode; both passed before the deploy.

## What was built on 2026-09-12 (evening batch) — and what Ash still has to switch on

Everything from the ideas list below numbered 15–30 is now in the code and deployed. How each one shows up:

| Feature | Client sees | Admin does | Needs from Ash |
|---|---|---|---|
| Live time slots | Exact free times per day once hours exist; "fully booked" + waitlist button otherwise | Calendar tab: opening hours per city (or per therapist), days off, a "free times on a day" preview | Nothing. Add hours when Zen is ready; until then the old window flow runs |
| Therapist profiles | Team on the booking page, therapist choice at booking, preferred therapist in the profile | Team tab: add name, one line, languages, photo; pause/remove; assign a therapist on a booking | Nothing |
| Session packs | Packs tab in the account, "Use my pack" at booking | Packs & partners tab: create packs (owner), see what's sold | Stripe live (packs are paid by card; the 2% is taken once on the pack) |
| Gift a session | New section on the site; code by email; `?gift=CODE` prefills the booking form | Gift vouchers list | Stripe live |
| Partner / gym codes | "Partner or promo code" box in the booking form | Codes per partner (all cities or one), report: bookings, this month, revenue, last booking | Nothing |
| Birthday reward | 50% off one session, issued by itself 10 days before the birthday, valid until 5 days after, once a year (credit expires after) | Birthday % in Settings → Rewards rules | Nothing (cron does it) |
| Reminders + follow-ups | WhatsApp message (once templates are approved) or email the day before, "how do you feel?" two days after | Message log in each client's drawer | WhatsApp Cloud API: `WA_PHONE_ID` var, `WA_TOKEN` secret, approved templates `zen_reminder` / `zen_followup` in Meta Business. Until then: email |
| Waitlist | "Tell me if a slot opens" on a full day; list in Sessions | Waiting list in the Calendar tab | Nothing (cron emails when a time frees up) |
| Health-flag review | Flagged clients (questionnaire) or anyone who ticks the safety box book without paying; they pay at the session after a therapist's OK | "Needs review" tab with a red counter: approve or cancel; approve a client once and they book normally after | Nothing |
| Before/after photos | Progress tab → "Before and after" gallery | Client drawer: upload (resized in the browser), kind, note, consent | Stored in D1 today. **Enable R2** in the Cloudflare dashboard (R2 → accept) and the next deploy moves new photos to the `zen-photos` bucket automatically |
| Export my data | Profile → "Print a summary" (export.html) and "Download (JSON)" | — | Nothing |
| Google review after 5★ | After a 5-star rating: "Leave a Google review" button | Settings → Links per city: Google review link, Google Maps link, WhatsApp number | Paste the links (owner) |
| Referral leaderboard | — | Overview → "Who brings friends" (owner) | Nothing |
| Apple sign-in | "Continue with Apple" (hidden until configured) | — | Apple Developer account: Services ID → `APPLE_CLIENT_ID`, `APPLE_TEAM_ID`, `APPLE_KEY_ID` vars in `wrangler.toml`; `.p8` key contents as the `APPLE_PRIVATE_KEY` GitHub secret; return URL `https://zenrecovery.club/api/auth/apple/callback` |
| SMS sign-in link | "Prefer a text?" box (hidden until configured); works for accounts that already have a phone | — | Twilio: `TWILIO_FROM` var, `TWILIO_SID` + `TWILIO_TOKEN` secrets |
| Head Chef line | — | — | Done: the daily rundown now reads Zen's D1 for bookings, ratings and the 2% |

**Username + email, own schedule (2026-09-12, night).** Admins have an `email` and an optional `username` (migration 012, unique when set); sign-in accepts either with the same password. Everyone edits both under Settings → My profile (a username is 3–24 of a-z 0-9 . _ -; an email must look like one; you can't drop a username while the account has no email); the owner can set a username when creating an account or later. Accounts that used a plain username in the `email` column (e.g. `owner`) keep working: the profile shows it under Username and lets them add an email. **Schedule:** a therapist's account sets and removes only its own opening hours and days off (the API forces `therapist_id` to their profile, whatever the body says; deleting the room's rows or a colleague's answers 403). In their Calendar the therapist filter is locked to them, the "Who" choosers show "You · Name", the room's strips and ranges appear read-only ("set by the owner"), colleagues' don't appear. An unlinked city account still manages the room's rows. The weekly hours are the standing schedule; anything different on a specific date is a day off (whole day or hours), also from the month view. **Motion layer, whole site (2026-09-13, Ash: "animate everything").** `public/motion.js` + `motion.css`, loaded by every public page after GSAP + ScrollTrigger (now included on giftcard, rewards, guide and account too). What runs: a short charcoal **curtain** with the round logo when a page opens, and a wipe before leaving through any internal link (back/forward restores cleanly); the nav slides in and gets a shadow once scrolled; **every heading** in `main` splits into words that rise one by one as it scrolls into view; **every paragraph, list item, card, button, image, field and method** rises into place, staggered per section (containers animate, not each line inside them; hidden and dialog content is left alone); photos **drift** while they cross the screen; buttons **lean toward the pointer** and spring back; a soft blue **aura follows the pointer** on desktop; numbers marked `data-count` count up; `.marquee` strips roll forever (home has one under the hero: methods and cities); a slowly moving aura sits behind the hero (the four floating rings that drifted over it were removed the same day: Ash, "take off the bubbles"). A MutationObserver re-scans when pages render later (catalog, team, language switch); it ignores text updates, the curtain and the aura, and only refreshes ScrollTrigger when something new was tagged, because a refresh cancels any smooth scroll in progress (that stalled the home cup story and anchor links the first time round). **Team order fix (2026-09-13):** the public `/api/team` never returned `sort`, so the live Team page fell back to alphabetical (Adham showed as 02 instead of Shaarawy) while the demo data, which carries `sort`, hid it; the API now returns `sort` and the smoke test requires it. Reduced motion or no GSAP: nothing runs, the page is plainly visible (all hidden states are set by the script). Playwright `motion.mjs` checks all six pages at desktop and phone width: engine on, curtain gone, headings split, items visible on screen and after scrolling, no overflow, link curtain then navigation, reduced-motion static.

**Everything in the visitor's language (2026-09-13, Ash).** Two halves. **Site:** team profile texts (title, bio, story, certifications, languages, neighbourhood) now exist in Italian and Arabic too: `therapists.i18n` (migration 014) holds `{it:{…}, ar:{…}}`; `/api/team` returns it and `team.js` / `booking.js` show the visitor's language with the original as fallback. The six current profiles were translated by hand and written to D1. From now on **Workers AI** (`[ai]` binding, model `@cf/meta/m2m100-1.2b`) translates automatically when the owner saves a profile whose texts changed, and the hourly cron (`translations`) fills any profile still missing them. The name heading is `dir="auto"` so Latin names don't reverse in Arabic. **Emails:** every client email lives in `src/mail.js` in en/it/ar (magic link, welcome, booking confirmation, review notice, cleared, reminder, follow-up, birthday, waitlist, package ready, gift buyer, gift recipient, referral reward, loyalty reward); the language is the one the client was browsing in when they booked or bought (`bookings.lang`, `gifts.lang`, sent by the pages as `lang`), else their profile language, else English. Admin notifications stay English.

**Admin polish (2026-09-12, late night).** Ash's screenshot showed a vertical scrollbar next to the tabs on Windows: `.tabs` had `overflow-x:auto` (so `overflow-y` became auto too) and the buttons hung 1px below the strip (`margin-bottom:-1px` for the underline), enough for Chrome on Windows to draw a scrollbar. Now: `overflow-y:hidden`, scrollbars hidden on the tab strip, the selected underline is an inset box-shadow (no overflow), the strip sticks under the top bar (`--top-h` measured by JS) and snaps on phones. Table wrappers use thin scrollbars; chart cards no longer stretch to the tallest neighbour; section heads tightened. **Bookings list is paged**: 30 rows at a time with "Showing 30 of N" and "Show 30 more" (the API still caps at the latest 300); status actions keep the current page.

**Three levels: owner, partner, employee (2026-09-12, late night, Ash).** `admins.level` (migration 013): owner = role `all`; a city account is a **partner** (runs the city: every booking there and assigning them, the whole team incl. add/edit/pause/remove, room and everyone's hours and days off, clients; read-only services and offers; no sign-in/link management, no prices, no settings) or an **employee** (one therapist: own + unassigned bookings and accepting them, own hours and days off, own profile only — no pause/order/remove, no adding people; no Services or Packs tabs). The platform account has no level. **Only the owner sets levels** (Settings → Who can sign in: city + level per account, and on creation); the people never see their level anywhere (`pub()` sends it so the page can hide tabs, nothing displays it). Services and prices became owner-only for everyone else. Enforced in `lib.js` (`isPartner`, `isEmployee`, `managesCity`, `visibleWhere`), `worker.js` (bookings, clients, admins) and `features.js` (therapists, schedule, services); mirrored in `demo.js`; tests in `smoke.mjs` and `levels.mjs`. Production: every city account was set to employee; the owner promotes partners himself.

**Team page v2 (2026-09-12, late night, Ash: "where is our AI magic").** Everyone is a full card now (Shika included), in one order across cities: the founder first (any title containing "Founder"), then the owner's "Order on the Team page" number, then name. Production sort set to Mazen, Shaarawy, Hesham, Adham, Anas, Shika. Cards alternate photo left/right, carry a number and a city tag on the photo, a hand-drawn underline under the name, story paragraphs (or the bio), the certificate checklist, and the buttons. **Motion** (GSAP + ScrollTrigger from cdnjs, the same build the home page uses): hero words rise one by one; each card as it scrolls in opens the photo with a curtain wipe from the side it sits on while the image settles from a zoom, then role, name words, underline draw, meta, paragraphs, certificates (with a small bounce) and buttons follow in sequence; the photo drifts slowly (parallax) while the card crosses the screen and leans toward the pointer on hover. City chips filter the list. Reduced-motion users and anyone whose GSAP fails to load get the static page, fully visible (initial hidden states are set by JS only). The Playwright suite `teampage.mjs` routes the CDN scripts to a local GSAP copy and checks order, entrance, filter, phone width, RTL and reduced motion.

**Delivering sign-ins (2026-09-12, late night).** Ash: accounts created in the admin "didn't work" for Adham. Most likely cause: the password inputs were plain text fields, and a phone keyboard capitalises the first letter (or autocorrects) while the owner types the new password, so the stored password isn't what he thinks he typed. Fixes: every password-creation input has `autocapitalize="none" autocorrect="off" spellcheck="false"`; a **suggest one** link fills a readable password (three words and two digits, e.g. `calm-cup-river-47`, same generator server-side); after creating an account or resetting a password the admin shows a **credentials dialog** with the exact sign-in and password ("write it down, not shown again") and, when the account has a real email, **Email it to them instead**; "Who can sign in" has an **Email sign-in** action per email account. Endpoint `POST /api/admin/admins/:id/invite` (owner only): sets a fresh temporary password and emails it (Resend) with the sign-in name; for a username-only account it returns the password once instead. `POST /api/admin/admins` now returns the new `id`; therapist save returns `admin_id`.

**Sign-in names are unique across both columns** (`signinTaken`): a username can't equal another account's email or legacy username and vice versa; login matches the email column first, then username. The duplicate `shika` account (never used) was deleted in D1 on Ash's request; the admins list stacks as cards on phones so Remove is never off-screen, and removal errors are shown in a dialog. **Promo code:** Ash's `mazen50` didn't work because it was saved as `MAZEN5O` (letter O); fixed in D1.

**Who sees which booking (2026-09-12, night, Ash's rule).** The owner sees and assigns everything. Any other account sees only the bookings **assigned to its linked therapist** plus the **unassigned** ones in its city (client chose "anyone available", or a hand-added booking with no therapist); an unlinked city account sees unassigned ones only. Opening an unassigned booking shows **Accept this booking** (`POST /api/admin/bookings/:id/accept`, first come first served, idempotent, 409 if someone else took it, city must match the profile). A therapist may update their own bookings (status, hour, notes) but `therapist_id` in a PATCH is refused with 403 unless it's the owner (or equals the self-accept). Bookings, the month calendar, the Needs-review list and the Overview numbers/today list all follow the same filter (`visibleWhere` in `lib.js`); packs/gifts stats stay per city. Bookings filter for therapist accounts: "Mine and unassigned" / "Only mine" / "Only unassigned". **Notifications** follow suit: a booking with a chosen therapist emails the owner(s) and that therapist's account only; an unassigned one emails the whole city so someone accepts it (`notifyList(env, city, therapistId)`). Hesham Khaled's profile (Cairo, Gold's Gym Beverly Hills, Instagram `heshamkhaled9016`) was filled the same evening; portrait at `img/team/hesham.jpg`.

**Team page (2026-09-12, night).** `/team` (`public/team.html` + `team.js`, "Team" in every nav and footer, in the sitemap): everyone from `/api/team`. A therapist with a **story** gets a large card at the top (photo, title, story paragraphs, certifications as a checklist, where they work with a map link, "Book with {first name}" → `/booking?city=…&therapist=…` which preselects them, "Follow on Instagram"); the rest are grouped **by city** as compact cards. New therapist fields (migration 011): `title`, `story` (paragraphs, line breaks kept), `certs` (one per line), `instagram` (handle, normalised from a URL or @). All editable under Team → "Public profile". Seeded in production: **Mazen Emad** (founder; Cairo, Sheikh Zayed · Gold's Gym Beverly Hills; photo `img/team/mazen.jpg` cropped from the certificate photo Ash sent; story and certificates from the Nogoom Al Arab article and the two certificates). The owner had meanwhile created his own therapist rows from the admin (Mazen linked to the `owner` account, Hesham Khaled, Adham in Cairo; Shaarawy in Dahab; Shika in Florence), so the seeded duplicate was deleted and Mazen's story/certificates/place were merged into the owner's row (his own uploaded photo kept). Shaarawy's Dahab row got title, story and Instagram (`recoverywithshaarawy`); a cropped portrait is at `img/team/shaarawy.jpg` if the owner wants to swap the one he uploaded. Ash confirmed (2026-09-12): Shaarawy is the one who does the cupping in Dahab, so his profile is Dahab only. Adham Aboklela (Cairo) works from Hansa Medica in New Cairo (Fifth Settlement, El Zohour Club / Medical Park; also Saray El Kobba) and is the recovery specialist of the Egyptian sitting volleyball team: title, story, Instagram, area, address and map link set on his row; portrait at `img/team/adham.jpg`. Anas Hany (Cairo, Gold's Gym Beverly Hills, Sheikh Zayed; Instagram `_anashany`; EFR first aid, PADI/SSI rescue diver) was added as a new row with `img/team/anas.jpg` as his photo (served by the deploy).

**Month calendar per city + where a therapist works (2026-09-12, night).** The Calendar tab now opens on a real **month grid**, one city at a time (city pills; a city admin sees their city only), with a therapist filter ("Everyone" / each therapist). Each day shows the number of sessions (yellow when one needs a therapist's OK), the first names and times, "day off" with its reason, and closed days shaded from the opening hours. Clicking a day opens a panel: the day's sessions (click one to open the booking drawer), free times as the client would see them, days off with remove, and three actions: **Add booking**, **Block whole day** (in-page dialog with a reason) and **Block hours** (opens the days-off dialog prefilled). The week strips and day-off ranges moved under the second pill, "Opening hours & days off", filtered by the selected city. Therapist profiles gained **neighbourhood** (shown at checkout next to the name, e.g. "Works in Maadi"), **street address** (confirmation email only: "Where: …", falling back to the city address from Settings) and an optional **Google Maps link** (`area`, `address`, `maps_url`, migration 010). At checkout the therapist whose neighbourhood matches what the client wrote as their town/area in their profile comes first with a "near you" tag; the dropdown carries the neighbourhood too.

**Calendar redesign (2026-09-12, late).** Opening hours are no longer a row-per-day table: one **week strip per room / therapist** (seven day cells, "closed" where nothing is set, an × on each hour block, a one-line summary such as "Mon–Sat 10:00–18:00 · 60 min", and "Remove all" for the whole strip). Days off are **merged into ranges** ("23 Dec – 2 Jan · 11 days · Florence · whole room · Christmas") under month headings, one Remove per range (confirmation when it's more than a day). Adding hours or days off happens in a dialog (`Add hours`, `Add days off`), so the page itself is only the current state. New endpoints `POST /api/admin/{blocked,availability}/remove {ids}` remove many rows at once, city-scoped. On phones the strip becomes a vertical list of days.

**Team ↔ sign-in accounts (2026-09-12, late).** A therapist profile (Team tab: photo, bio, languages, order) can be linked to the admin account the person signs in with (`therapists.admin_id`, migration 009). The owner does it from the therapist dialog under **Sign-in account**: pick an existing account that can see that city, or **create a new sign-in right there** (username or email + password; the account gets the therapist's name and is locked to their city). One account per profile and vice versa: linking an account elsewhere moves it; removing an account, or narrowing it to another city, unlinks it; the platform account can't be linked. A linked person sees "this is you" on their card, Bookings opens on **Only my sessions**, and Settings → My profile points them to Team for the public photo/bio. "Who can sign in" shows each account's therapist profile. City admins see that a link exists but not the account behind it, and can't change it. Every browser pop-up in the admin (confirm/prompt/alert) was replaced by an in-page dialog the same evening.

**Owner account (2026-09-12 evening).** Zen's owner signs in with username `owner`; the first password was given to Ash in chat, so the owner must change it under Settings → Your password on first sign-in. Ash's own account (fetta.amore.business@gmail.com) is now `platform`: numbers only.

**Still open from before:** roll the Cloudflare API token that was pasted in chat (Cloudflare → My Profile → API Tokens → roll, then update the `CLOUDFLARE_API_TOKEN` GitHub secret); change the owner's admin password (Settings → Your password); decide the Stripe Connect entity for Egypt (see above) and set `ZEN_STRIPE_ACCOUNT` + the Stripe secrets to switch payments on; add Google OAuth client id/secret for the Google button.

## Tests: the pages talking to the real API (added 2026-09-13)

Ash (2026-09-13): "it seems like the pages are not communicating between each other". The Team-order bug proved the gap: every browser suite ran the pages against `demo.js` sample data, which carried fields the real API didn't, so a missing field on the live API was invisible. Now there is a real end-to-end pass in `test/`:

- `node test/e2e-server.mjs` — serves `public/` on :8766 and routes `/api/*` into the actual `src/worker.js`, with an in-memory SQLite standing in for D1 (same shim as the smoke test), the live team seeded in the owner's order, an owner account, `DEV_MAGIC_LINK` on. `demo.js` never kicks in because `/api` answers.
- `node test/e2e.mjs` — Playwright, one browser, in this order: every internal link on the eight public pages answers 200 and an unknown path gets the 404 page; the Team page shows the API's order (founder, then `sort`); "Book with …" lands on booking with the city chosen and the therapist preselected; services come from `/api/catalog`, the therapist list and the "where they work" cards from `/api/team`, slot mode from `/api/slots`; the summary fills; the checkout answer reaches the client (dialog or message, button re-enabled); a language chosen on the home page is kept on booking, team and gift (`lang`/`dir`); gift services and rewards numbers come from the API; a first sign-in through the magic link opens the intake wizard and the booking page then knows the client; the owner adds a therapist through the admin API and the Team page + booking list follow, a paused therapist disappears; success page renders; no JS errors.
- Runs on 2026-09-13: e2e 31/31 after the `sort` fix; smoke 227; anim 8; motion 89; the other browser suites (ui, mobile, pages, others, teampage, methods, covers, city, svc) unchanged.

**UI/UX pass (2026-09-13, `ui-ux-pro-max` review).** Skip link on every public page (`.skip`, shows on keyboard focus); every error message is `role="alert"`; `touch-action: manipulation` on controls, buttons ≥44px tall (small ≥40px), language switch and chips ≥40px on touch screens; nav CTA never wraps on phones (compact spacing under 430px); the icon-only account link has an accessible name; photos carry `width`/`height` (no layout shift) and lazy-load below the fold; press feedback on buttons and choices; inline validation colour only after the person has typed (`:user-invalid`); submit buttons show "One moment…" with `aria-busy` and get their label back; the "Google sign-in isn't set up yet" developer note no longer shows to visitors. The skill's suggested pink/lavender palette and Lora/Raleway pairing were **not** adopted: the brand is the charcoal + blue of the logo and Ash has not asked for a rebrand. Its "social proof before the CTA" pattern is logged as an idea for Ash (real client reviews on the home page, never invented ones).

## Near you: the site as the connection between therapists and clients (2026-09-13, Ash)

Ash: "all the therapists would be like providers of the service and the clients can search on where the service would be based on their location." So every therapist is now a **provider with a pinned place**, and a client can start from where they are instead of from a city.

- **Data** (migration 015, applied to production): `therapists.lat`, `lng` (the pin) and `radius_km` (how far they travel to clients; 0 = at their place only). `/api/team` returns them. The six live therapists were pinned from their known workplaces (Gold's Gym Beverly Hills · Sheikh Zayed for Mazen, Hesham and Anas; Hansa Medica · New Cairo for Adham; Dahab for Shaarawy; Florence for Shika) — **approximate to a couple of km**: each therapist should open Team → their profile → "Use my current position" once, while at their place, to make it exact.
- **API** `GET /api/providers?lat=&lng=&q=` — every active therapist across the three cities, with `km` (great-circle distance from the client) when a position is given, `pinned`, `comes_to_you` (within their radius), the city's `from` price, `currency` and number of `services`; nearest first, unpinned last; `q` filters on name, neighbourhood, title, city (in every language); `cities` with the distance to each city centre and `nearest`. Nothing is stored: the position is only a query parameter.
- **Admin** Team → profile: "Pin on the map" (Use my current position / paste latitude + longitude; a Google Maps link with coordinates in it fills the pin by itself), "Also travels to clients within (km)". The team card says "not pinned on the map" until it is.
- **Public** `find.html` ("Near you", in the nav on every page and the home hero's first button): **Use my location** (browser geolocation, nothing leaves the phone except the one API call) or type a neighbourhood/city; city chips; cards nearest first with "x km away", "Comes to you", works-in + Map, languages, "From EGP 700 · 6 sessions", Book with → booking with the therapist preselected, "Their story" → team. The position is kept in `localStorage` (`zen:loc`, 7 days) so **booking** sorts its therapist list by real distance (and shows "x km from you" + "Comes to you") and the **Team page** shows a distance badge. Without a position: everyone, sorted by the owner's order, and the "near you" text match on the profile's neighbourhood as before.
- **Not built yet (Ash decides):** therapists signing themselves up as providers (today the owner creates every account), a map view (needs Google Maps JS, an external script), and per-therapist prices (prices are still per city).
- **Tests:** smoke +10 (pins, radius, distance sort, maps-link coordinates, bad pins cleared, text search); e2e +8 (find page without a position, typed area filter, granted geolocation → nearest first with km, nearest city, Book with link, booking sorted by distance with km, team badge); motion suite covers `find.html`.

## Admin sweep against the real API (2026-09-13, Ash: "recheck everything, buttons, features, functions")

`test/admin-e2e.mjs` (needs `test/e2e-server.mjs`): the owner signs in through the form, opens every tab, works the calendar (city pills, therapist pills, month prev/next/today, a day → day view, block day, block hours, opening hours and days-off dialogs, add booking from the day view), the Team dialog (pin controls, an existing pin prefilled, radius saved and visible on the API), services and settings, creates a sign-in for a therapist from their profile, signs out, signs in as that employee and checks the own-calendar badge and that only their own profile opens. No JS errors, no native pop-ups. Two real bugs came out of it, both fixed:

- **Calendar therapist pills didn't react.** Clicking a name changed the filter but nothing showed it: the pressed state stayed on "Everyone" (and on an empty month there was nothing else to change). The pressed pill now moves on click. Paused therapists also no longer appear as pills unless they still have sessions in view.
- **"Accounts created from the Team dialog don't work."** The credentials dialog's main (dark) button was "Email it to them instead", which silently replaced the password the owner had just typed (and probably already told the person) with a fresh temporary one. Now "Got it" is the main button, "Also email it to them" is a secondary one and it emails **the same password** (`POST /api/admin/admins/:id/invite` with `{ password }` keeps it when it matches; without one it resets as before, which is what the Settings "email a new sign-in" button does on purpose). Smoke +4, admin sweep 30.

## Services in three languages + a therapist's own prices (2026-09-13, Ash's batch, slice 1)

- **Services translated.** `services.i18n` (migration 016) holds `{it,ar}` for name and description, filled by Workers AI when the owner saves a service with changed texts, and backfilled by the hourly cron (5 per run; the 16 live services fill in within the first hours after deploy). `/api/catalog` sends `i18n` per service; booking (cards, summary), gift options and the emails' session line use the visitor's language. Standard names keep the dictionary fallback in `i18n.js`.
- **Per-therapist prices.** `therapist_prices` (therapist_id, service_id, amount). Team → profile → "Their own prices": the owner or the city's partner types a price per session (empty = city price); the therapist sees theirs read-only. `/api/catalog` carries `prices: {therapist_id: amount}` per service; the booking page shows the chosen therapist's price on the cards and in the summary ("Prices can differ by therapist." under the choice), and back to the city price for "Anyone available". Checkout charges the own price **only when the client chose that therapist**; an automatic assignment keeps the city price so nobody pays more than they saw. Near you's "From" price is the minimum across that therapist's own prices.
- **Fixed on the way:** in window mode (no availability calendar) the therapist chosen on the Team page or in the form was dropped at checkout; it is now kept, so "Book with Mazen" reaches Mazen.
- Tests: smoke +12, e2e +2 (Adham's 1,200 vs the city's 1,000).

## Booking rules, cancellation fees, self-cancel, waitlist fill (2026-09-13, slice 2)

Ash asked for the cancellation rules "from GetYourGuide if they apply". What applies to a one-to-one session, and is now the site's policy: **free cancellation and free moves up to 24 h before**; inside that window **100% is kept**; **no-show 100%**; the therapist cancelling means a full refund or a new time; late arrival shortens the session; a session sent for a therapist's OK is not charged until approved; refunds go back to the card within 5–10 days. The numbers are settings (`cancel_hours`, `late_pct`, `noshow_pct`, migration 017, applied to production) that the owner edits under Settings → Rules; `/api/status` publishes them as `rules`.

- **Where they show:** a "Booking rules" box on the booking form with a required "I've read the booking rules" tick (`bookings.agreed_at` records it), a "Booking rules" section on the guide, and the cancellation email.
- **Client self-cancel:** My account → upcoming session → Cancel. `GET /api/me/bookings/:id/cancel` previews the terms (late?, fee, refund); `POST` cancels. The fee is computed from the city's local time to the start of the slot (exact HH:MM, or 9:00 / 12:00 / 17:00 for the morning / afternoon / evening windows). A free cancellation returns the reward, gift or package session; a late one keeps it, like the fee.
- **Refunds:** `stripeRefund` refunds the remainder on Zen's connected account with the platform fee refunded proportionally, when Stripe is live and the booking was paid online; otherwise `refund_status = manual` and the therapists' email says "TO ARRANGE". No-shows refund nothing.
- **Admin:** booking drawer → "Cancel (rules)" (with an optional fee % override) and "No-show". The admin list carries `cancel_fee`, `refund_amount`, `refund_status`, `cancelled_by`.
- **Waitlist fill:** every cancellation (client or admin) emails the first three people waiting for that city and day, once each, with a booking link, and logs it; the hourly cron keeps doing the same for days that free up otherwise.
- Tests: smoke +15 (rules, owner edits, free vs late, 50% fee + manual refund, no-show 80%, twice refused, waitlist notified), e2e +6 (rules box, pay refused until agreed, Cancel from the account, dialog says no fee, card gone).

## Fawry for Egypt (2026-09-13, Ash: "add Fawry for the Egyptian transactions, deposited in an Egyptian account")

**How the money flows now.** Two providers, chosen by currency: **Fawry for EGP** (Cairo, Dahab) as soon as it is configured, **Stripe for EUR** (Florence) once Connect is live. Fawry settles to the Egyptian bank account on Zen's FawryPay merchant account (Ash's family's, as decided). There is no automatic split with Fawry: the platform's 2% is **recorded** on every Fawry booking (`platform_fee`) and collected through the monthly statement per therapist, not by the gateway.

**Integration** (`src/lib.js`: `fawryOn`, `payProvider`, `fawryCheckout`, `fawryStatus`, `fawryNotificationValid`; `src/worker.js`: `/api/fawry/return`, `/api/fawry/notify`, `fawrySettle`):
- Booking, gift and package checkouts call FawryPay's hosted checkout (`POST {base}/fawrypay-api/api/payments/init`, signature = SHA-256 of merchantCode + merchantRefNum + customerProfileId + returnUrl + itemId + quantity + price(2 dp) + secure key) and send the client to the page Fawry returns. `merchantRefNum` is `bk`/`gf`/`pk` + our id, stored in `stripe_session` with `provider = 'fawry'` (migration 018, applied to production).
- The client comes back on `/api/fawry/return`; Fawry also POSTs `/api/fawry/notify` (server notification V2, `messageSignature` checked against our secure key; the V1 shape is accepted too). **Neither is trusted alone**: every settlement re-reads the order with Get Payment Status V2 (`/ECommerceWeb/Fawry/payments/status/v2`, signature merchantCode + merchantRefNumber + key) and only `PAID` marks the booking paid, then the same confirmation emails and rewards as a Stripe payment. Repeated notifications are harmless. EXPIRED / CANCELED notifications cancel a still-pending booking.
- Refunds on Fawry payments are **manual** for now (`refund_status = manual`, the therapists' email says so); Fawry's refund API can be wired later.
- Base URL: `FAWRY_ENV = staging` → `atfawry.fawrystaging.com`, `production` → `atfawry.com`. The booking button reads "Reserve and pay with Fawry" with "Card, mobile wallet or a Fawry reference number, in EGP".
- **Not verifiable from this session:** developer.fawrystaging.com is blocked by the egress policy, so the request/response shapes come from FawryPay's published docs and SDK samples as indexed by search. The first real transaction must be done on **staging** (test cards from the FawryPay dashboard); if the init endpoint answers with a JSON object instead of a plain URL, `fawryCheckout` already reads `url` / `paymentUrl` / `redirectUrl`.

**Ash's checklist to switch it on:**
1. Open a FawryPay merchant account (fawry.com → FawryPay → "Get started"; company documents + the Egyptian bank account for settlement). Ask for the **staging** credentials first.
2. In the FawryPay dashboard: copy the **merchant code** and the **secure key**; set the server notification (callback) URL to `https://zenrecovery.club/api/fawry/notify` and allow the return URL `https://zenrecovery.club/api/fawry/return`.
3. GitHub → repo → Settings → Secrets → `FAWRY_SECURE_KEY`; `sites/zen/wrangler.toml` → `FAWRY_MERCHANT_CODE` (and `FAWRY_ENV = "staging"` until the first real payment works; then `"production"` and the production code/key).
4. Deploy (any merge does it), book a Cairo session, pay on Fawry's page, check the booking turns "paid" in the admin and the confirmation email arrives.
- Tests: smoke +12 with Fawry's endpoints mocked (init signature, status re-read, signed/unsigned notification, return before/after payment, gift in Dahab, Florence untouched).

## Body map on the booking form + "What clients say" (2026-09-13, slice 3)

- **Where does it hurt?** The booking form shows the same back-view body map as the client's intake (`ZenGuide.bodyMap`, keyboard-reachable parts with `aria-pressed`); tapped parts become chips and go to checkout as `areas` (whitelisted against the map's part keys, de-duplicated, `bookings.areas` JSON, migration 019 applied). The therapists' new-booking email starts with "Where it hurts: …" and the admin booking drawer shows the map and the chips.
- **What clients say.** Real ratings only: after a done session a client leaves 1–5 stars and words in their account; in the booking drawer the **owner** ticks "Show on the home page" on a 4- or 5-star rating with words (`bookings.featured`, owner-only PATCH). `GET /api/reviews` (cached 5 min) returns first name, city, session type, stars and the words; the home page's "What clients say" strip (between the room photos and the links) renders them in a scroll-snap row and stays hidden while nothing is picked. Nothing is ever written by us.
- Tests: smoke +9, e2e +5.

## Provider sign-up with the owner's approval (2026-09-13, slice 4)

- **Public page `join.html`** ("Join as a therapist": footer link on every page, CTA on Team and Near you, in the sitemap; EN/IT/AR). Name, email (their future sign-in), WhatsApp, city, title, languages, neighbourhood, address, Maps link, Instagram, one-line bio, story, certificates, a photo, a map pin (same "Use my current position" as the admin), travel radius, and a terms checkbox. `POST /api/apply` validates, stores an `applications` row (migration `020`, applied to production 2026-09-13), emails the applicant a "we got it" (template `application_received`) and emails every owner account a summary with a link to the admin. One open application per email.
- **Admin → Team → Applications** (owner only; a count badge while someone is waiting; filter new/approved/declined). Each card shows what they wrote plus the story and certificates folded away. **Approve** asks first (optional note), then: a sign-in account for that email (role = their city, level = employee, so they see only their own bookings; if the email already has an account it's reused), the public profile linked to it, pinned where they said, at the end of the Team order, at the city's prices (per-therapist prices can be set afterwards), profile fields translated to IT/AR, and an email with the sign-in (`application_approved`). If the email can't be sent the password is shown once in the credentials dialog, exactly like adding a therapist by hand. **Decline** asks for an optional reason the applicant reads (`application_declined`). Approved applications keep an "Open profile" shortcut.
- Nothing is public until the owner approves: an application is not a therapist, appears nowhere on the site, and can't sign in.
- Tests: smoke +11 (validation, duplicate, owner-only list, approve creates admin + therapist + i18n, decline, already-decided), e2e +5 (form → thank-you → owner's list, duplicate refused), admin-e2e +10 (list, decline with note, approve → credentials → team card → she signs in, employee sees no box).

## Statements per therapist, "Your earnings", and codes the team hands out themselves (2026-09-13, slice 5)

- **Statements** (admin tab, owner and partner-level; `GET /api/admin/statements?month=YYYY-MM&city=&detail=1`). One card per therapist and currency for the month: sessions (done / to come / fee kept), then the money by how it was taken: **card** (Stripe: on Zen's Stripe account, the 2% already taken by Stripe), **Fawry** (on the Egyptian settlement account, the 2% recorded and settled through this statement, as the Fawry section says), **cash** (entered by hand: the therapist collected it, no platform fee), **free** (pack, gift, reward). Late cancellations and no-shows count with the fee kept, under their session's channel. Bottom lines: gross, Amico Mio's 2%, **to pay the therapist** = card + Fawry − 2%, with cash shown separately as already in their pocket. A session table underneath and **Download CSV** (`&format=csv`, one line per session, UTF-8 with BOM so Excel opens it) for the accountant. Bookings that are still to come are included, so a month reads the same before and after.
- **Your earnings** (the same tab for an employee sign-in): only their own sessions and their own card, always with the session list. Partner-level accounts are locked to their city; the owner has city pills.
- **Codes without us**: partner codes now have a **kind** (partner, hotel/hostel, gym, company) and a contact. The owner *and* every city's partner-level admin create, pause, edit and remove codes in Packs & partners → Partner codes; a partner-level admin is locked to their own city and sees their city's codes (not the owner's all-cities ones). The report shows bookings, people, this month, revenue per currency. Migration `021` (applied to production 2026-09-13). The public lookup carries the kind; the booking form's code field is unchanged ("Partner or promo code").
- Not built on purpose: no automatic monthly email of the statement to therapists. The numbers are in the admin the moment they're needed; a scheduled send is a one-line cron addition once Ash decides who should receive it.
- Tests: smoke +25 (codes by partner vs employee vs owner, city lock, kind on the public lookup, statement maths with one of everything, city and self scope, CSV, bad month), admin-e2e +9.

## In-site chat between client and therapist (2026-09-13, Ash: "a communication system between the client and the therapist")

- **Client side** (account → **Messages**, badge with unread count): a conversation per therapist, or with the city's room when no therapist is picked. "Write to" lists every active therapist (the ones they've booked first). Every therapist card on Near you and Team has a **Message** link, and each session in the account has "Message {first name}". `account.html?chat=<therapist id>` opens that conversation, also after a sign-in (the intent is kept in `localStorage`). Enter sends, Shift+Enter is a new line; the page refreshes itself every 15 s while open. Routes: `GET/POST /api/me/chats`, `GET/POST /api/me/chats/:id` (`?after=` for the delta).
- **Team side** (admin → **Messages**, badge refreshed every minute): a therapist sees their own conversations plus the room; the city's partner and the owner see every conversation in the city and can answer too. An answer is signed with the therapist's name when the writer is that therapist (or answers the room); otherwise the client sees "Zen · name". "Open client" jumps to the client drawer. Routes: `GET /api/admin/chats[?city=&count=1]`, `GET/POST /api/admin/chats/:id`. The platform account can't read chats.
- **Nobody is online all day**: when the client writes, the therapist's notify list gets an email (same list as booking notices); when the team answers, the client gets one in their language (template `chat_client`) with a link. Both are throttled to **one email per half hour per conversation**; the chat itself always shows everything. No WhatsApp or SMS: the existing "Message the room" WhatsApp link stays for urgent things.
- **Limits**: plain text, 2000 characters, blank lines collapsed, 30 messages per 10 minutes per client. Tables `chats` + `chat_messages` (migration `022`, applied to production 2026-09-13).
- **The pings are proper emails now** (2026-09-13 evening, Ash: "an email pops with urgent and the customer message, with a Respond button"): subject "Urgent · new message from {client} · {therapist}", the whole message in a card with the client's name, email and WhatsApp, and one **Respond in the chat** button that opens that very conversation in the admin (`admin.html#chat=<id>`). The client's email works the same way in their language, the button opening their conversation (`account.html?chat=<therapist>#messages`). Same half-hour throttle. Checked on production: Shika's inbox received both chat pings on 2026-09-13 (Resend: delivered).
- **Rebuilt WhatsApp-style the same evening** after Ash's phone screenshot (the first cut had its styles in `site.css`, which the account page doesn't load, so it rendered bare). Now one stylesheet block in `app.css` serves both sides: conversation list (avatar, name, time, one-line preview, green unread badge), full-screen thread on phones with a back button (the open thread is moved to `<body>` while open, because a motion-transformed ancestor would trap `position:fixed` under the sticky nav), day chips (Today / Yesterday / date), bubbles with tails, time inside the bubble, **read ticks** (grey ✓✓ delivered, blue once the other side opened the conversation or replied; `chats.client_read_at` / `team_read_at`, migration `023`, applied to production), the sender's name on bubbles that aren't from the therapist, a growing composer with a round send button, Enter sends on a keyboard and adds a line on a phone. On a phone the list shows first; on a desktop the latest conversation opens by itself. "New message" is a picker sheet with the therapists (booked first) and the room.
- Not built: attachments/photos in chat (the before/after photos already have their own place), read receipts, push notifications. Any of these is a small follow-up if Ash wants it.
- Tests: smoke +29, e2e +9, admin-e2e +10.

## Every therapist owns their list; clients choose by a brief (2026-09-13, Ash: "everyone should apply their numbers and control their list… the client should browse between them and choose the best one based on their profile in a small brief")

- **Their list** (admin → Team → their profile → "Your list and prices"): a therapist unticks the sessions they don't do and types their own price per session (empty = city price). Owner and the city's partner can edit anyone's; an employee only their own (`PUT /api/admin/therapists/:id/prices` with `prices` + `offered`; `therapist_prices.offered`, migration `024`, applied to production). A switched-off session: the therapist isn't offered for it on the booking page, their slots don't appear for it (`/api/slots?service=`), and a checkout naming them for it is refused.
- **Choose who you'd like to see** (booking page, any city with 2+ therapists, Cairo today): the therapist select becomes cards — photo, name, title, one-line bio, ★ average and number of ratings (real ones left after sessions) or "New on Zen", sessions done, languages, neighbourhood, distance and "Comes to you" when a position is known, the **price for the chosen session** (their own or the city's), links to their story and to the chat. An "Anyone available" card keeps the city price and the first free slot. Cards follow the chosen session: switching session re-filters and re-prices; a note says how many of the team don't offer it. `GET /api/team` carries `sessions_done`, `rating_avg`, `rating_n`, `not_offered`; the Team page shows the same brief line.
- Tests: smoke +12, e2e +8, admin-e2e +2.

## Glass buttons, the booking step by step, guests registered, one search box (2026-09-13, Ash)

- **Glass buttons** ("a kind of glass, futuristic, iPhone luxury feel"): one block at the end of `site.css` and `app.css` restyles `.btn`, `.btn.ghost`, `.pill`, the language switch and the chat send button — translucent gradient, a top highlight, inner bevel, blur behind, a soft drop shadow, press-down on tap, and a blue glass variant for the accent buttons. No markup changed: every button on the site picked it up.
- **The booking, page by page** (`booking.html` is now a wizard, `booking.js` bottom): 1 *Where it hurts* (body map + note) → 2 *Treatment* (before the therapist, because not everyone offers everything) → 3 *Therapist* (the chooser cards; skipped when a city has one therapist) → 4 *When* (day + time) → 5 *Your details* (name, WhatsApp, email, the safety question) → 6 *Rewards & codes* (shown to signed-in clients; a guest reaches it from "Have a gift or partner code?" on the last step) → 7 *Check and confirm* (summary, rules, agree, pay). A step bar on top (done steps are shortcuts back), Back / Continue at the bottom (sticky), Enter moves on and never submits early, each step validates before the next (day, time, name, WhatsApp, email). The `.no-motion` class keeps the motion layer off the wizard.
- **Whoever books is registered**: at checkout a guest's email becomes an account if it isn't one yet (name, WhatsApp, city, invite code), and the booking is theirs, so sessions and rewards are kept and a magic link signs them in later. Step 5 says so. A second booking with the same email lands on the same account.
- **One search box** in the admin's top bar (`/` or Ctrl/⌘-K focuses it; `GET /api/admin/search?q=`): clients (name, email, phone digits with spaces ignored), bookings (name, email, phone, session, date, id), team, partner/gym codes, gifts and applications, grouped, scoped like the rest of the admin (owner all, partner their city, therapist what they may see; the platform account can't). A hit opens the client drawer, the booking drawer, the therapist dialog or the right tab.
- **Storage**: everything the team stores is in D1 (SQLite) tables, one per kind of thing. Migration `025` (applied to production) adds the indexes the admin and the site actually look things up by: bookings by email / client / city+date+status / therapist+date / name / phone, users by phone / name / city, gifts by code and emails, applications by email, therapist prices by service. Full-text search (SQLite FTS5) is the next step if the notes and chat ever need searching by words.
- Tests: smoke +14, e2e +13 (the wizard walked step by step in a fresh, signed-out browser; older checks reveal all steps), admin-e2e +6.

## Ash's photos on the site + a phone pass (2026-09-13)

- **Photos** (`public/img`, resized with Pillow, progressive JPEG): `room-cairo.jpg` (the Cairo room, a client under cups, the logo on the wall) is the Cairo panel on the booking page and opens the "In the room" strip on the home page; `tool-glow.jpg` (myofascial tool under red light) and `deep-tissue.jpg` (thumb work on a calf) follow it, with the three earlier room shots after. Captions in EN/IT/AR. On phones the strip is a horizontal snap scroller with one card shape (4:5) so captions line up; on desktop a 3-column grid.
- **Phone pass** (`site.css`, "phone polish" block): a type floor at 640 px (eyebrows, mono labels and small print no longer under 11–12 px; body text 1.02 rem / 1.55 line height), 40 px tap targets on coarse pointers (plain links, footer links, story/message links, summary rows, checkboxes at 20 px), the body map centred and wider, the duplicate "Change city" button dropped on phones, chooser cards a touch tighter, Back/Continue sharing the row. A sweep of every public page at 390 px found no horizontal overflow. Real bug found on the way: the wizard's sections used the class `.step`, which the home page's dark "How it works" section also owns, so the step text rendered pale; the wizard uses `.wstep` now.

## A therapist's own services (2026-09-14, Ash: "why can't Shika and the other therapists change or add or delete products?")

- Until now the catalog was one list per city that only the owner edits; a therapist could only switch those sessions on or off and set their own price. Now **every sign-in linked to a therapist profile has a Services tab** with the city's list (read-only for them) and **"Add one of your own"**: name, minutes, price, one line, a cover photo. What they add is theirs: it shows on the booking page with a "With {first name} only" tag, only they get booked for it (the slots for it list them alone, a checkout naming anyone else is refused, an unassigned booking of it goes to them at that price), and they edit, hide or remove it themselves. The owner sees whose it is in the list and can edit or add one on a therapist's behalf. `services.therapist_id` (migration `026`, applied to production); NULL keeps meaning the city's list.
- Not changed: the city's list is still the owner's, so a therapist can't rename or reprice "Dry cupping · Cairo" for everyone; for that they set their own price and on/off in their profile, as before.
- Tests: smoke +13, e2e +3, admin-e2e +3.

## Emails for the team, the right country's rooms, private earnings, "where it hurts" over time (2026-09-14, Ash's four asks)

- **Therapists' emails.** Every team sign-in already edits its own email under Settings → My profile; what was missing was the push. Now a sign-in **without an email** (`owner`, `hesham`, `adham`, `anas`, `shaarawy` on production are username-only) sees a red **"Add your email so clients reach you"** banner on every admin screen, with a link to the profile; the owner's Team card says **"no email yet: bookings and client messages can't reach them"** next to that person. Nothing is collected from anywhere else: the email is the one they type on the site, and it becomes their sign-in and the address the booking and chat pings go to (`notifyList` only ever emails addresses with an @).
- **The right country's rooms** (Ash: "based on location force the client to be either in Egypt or in Florence… let them know we have a branch"). `/api/status` now carries `geo.country` from Cloudflare (`request.cf.country`, free, no lookup). On the booking page a visitor **in Egypt** sees Cairo and Dahab, with Florence dimmed and a line: "You're in Egypt, so we show the rooms in Egypt: Cairo and Dahab. Zen also has a studio in Florence, Italy. **Travelling there? Show all cities**"; **in Italy** it's the mirror (Florence open, Cairo and Dahab dimmed). Tapping a dimmed city shows the note instead of opening the booking; a `?city=` link into the wrong country is held back the same way and opens once they tap "Show all cities". The unlock is remembered on that phone (`localStorage zen:travel`), with a "Back to the rooms near me" link. Anywhere else in the world: all three cities, no note. The city preselects (a `?city=` link, the profile's nearest room) now wait for the status answer (at most 2.5 s) so the gate is known before a city opens. Test servers pass the country in an `x-e2e-country` header; the smoke test feeds `cf` on the request.
- **Private earnings** (Ash: "the owner shouldn't know how much everyone is making, neither do I… I keep my percentage and the total, not details"). `GET /api/admin/statements` now returns **one row per city and currency ("Cairo · team")** for the owner and the partners, with the same sums as before (sessions, card/Fawry/cash/free, fee, "to pay the team"); the session detail and the CSV lose the therapist column. A **therapist** still sees only their own line under "Your earnings", with their sessions and CSV. The **platform account** (Ash) gets the same totals under a **"Platform fee"** tab (it was locked out of statements before) and never the session list or the CSV (`403`). The admin's Money help now has "Who sees what". Consequence to be aware of: **the owner no longer has a per-person "to pay" figure in the admin**; each therapist reads theirs under "Your earnings" and can send the owner the CSV. Ash confirmed this is what he wants; flipping it back is a one-line change (`team = !me` in `statements()`).
- **Where it hurts, over time.** New table `pain_log` (migration 027, applied to production): one row per change of areas, from the **intake**, from **each booking's body map** (the booking's areas also overwrite the profile map, so the therapist's "Focus areas" and the client drawer's body are always the latest), or from the **client's own update**. Saving the same areas again adds nothing. Client side, the **Progress** tab has "Where it hurts, over time": a strip of mini bodies (date + where it came from), a tappable body to update now with **Save where it hurts now**, and a line comparing first visit and latest ("Better since your first visit: lower back. New: neck."). Admin side, the client drawer's "Their answers" shows the same strip and the same comparison. `/api/me` and `/api/admin/clients/:id` return `pain_log` (newest first, 12 max).
- Tests: smoke +21 (409), e2e +15 (113), admin-e2e +9 (88).

## Hardening batch (2026-09-14, Ash: "think about more enhancements, check everything, do what you see necessary")

Twenty-odd small things, each one either a safety net, a missing basic, or a nudge so the humans finish what only they can finish.

- **Stripe, as far as it can go from here.** Zen's connected account exists (`acct_1UFIhGFxTkRNsABQ`, Italy, Standard) and is now in `wrangler.toml` as `ZEN_STRIPE_ACCOUNT`. It is **not finished on Stripe's side**: nothing submitted, no bank account, charges and payouts disabled (checked 2026-09-14 through the platform account). And the GitHub secrets `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` are still empty (the deploy log says "skip … secret not set"). Card payments stay off until Zen's owner completes onboarding at dashboard.stripe.com and Ash adds the two secrets; the admin now says so on the overview (**"Card payments are off. Missing: …"**, owner and platform only) and `/api/admin/me` carries `pay: { live, stripe_key, stripe_account, stripe_webhook, fawry }`.
- **Throttling** (migration 028, table `attempts`): admin sign-in refuses after 8 wrong tries per sign-in name or 40 per connection in 15 minutes (only failures count); magic links max 6 per address and 60 per connection an hour; the join form 5 an hour per connection; error reports 20 an hour. `tooMany()` / `noteAttempt()` in `lib.js`.
- **Easy-password nudge:** sign-in returns `weak: true` for short letters-then-digits passwords or a few common ones; the admin shows a red banner until it's changed under Settings. (The owner's shared `owner12345` trips it.)
- **One booking per therapist per exact time**, enforced by the database (partial unique index `bookings_one_per_slot`, live statuses, exact `HH:MM` slots only; windows like "morning" can hold several). The availability check stays the first line; a race now answers 409 "just taken" instead of a double booking. Checked production first: no duplicates existed.
- **No double booking by the same person:** the same email, city, day and time while a live booking exists → 409 pointing at My account (a double tap on Pay, a second tab).
- **`/api/health`:** database ping, `live` flag; for Guardiano and the deploy's live check (which now also hits `/privacy`).
- **Browser errors reported by the pages** (`site.js` → `POST /api/log`, live site only, three per page view): stored in `client_errors`, same message on the same page counted once a day; the owner sees them on the overview ("Site errors", hidden when empty), the platform account through `GET /api/admin/errors`.
- **Security headers** in `public/_headers` (nosniff, referrer policy, frame-ancestors same origin, permissions policy, HSTS) and `X-Robots-Tag: noindex` on admin, account, export and success; every API answer is `nosniff`.
- **Privacy page** (`privacy.html`, EN/IT/AR with a language switch that follows the site language): what is stored, that the intake answers are health data kept with explicit consent, who processes what (Cloudflare, Resend, Stripe, Fawry, Google/Apple), retention, rights, cookies. Linked from every footer and in the sitemap. Wording says the site is run for Zen by Amico Mio (Florence); change it there if that isn't how Ash wants it stated.
- **Monthly statements by email** (cron, the 1st at 08:xx Cairo time): each therapist with an email gets their own line for the previous month, the owner/partners/platform get the totals per city, one email per person per month recorded in `messages` (kind `statement`). `sendStatements(env, month)` in `cron.js`.
- **A team member's new email gets a note** ("your notifications now come here… same password") the moment they save it, so a typo shows up as a missing email rather than missing bookings.
- **Confirmation emails carry an "add it to your calendar" link** (Google Calendar, exact-time bookings only, in the city's time zone). The account page already had the `.ics` download.
- **"Book again"** on every past session in the account, with the city, session and therapist prefilled.
- **JSON-LD** (Organization + the three rooms) on the home page, `/privacy` in the sitemap.
- **Translations:** a check over every `t("…")` string found two untranslated ones (the Near-you "no one matches" line and the booking's "fill in your name…" error); both added in IT and AR.
- **Typed errors:** a thrown error with a `status` now answers with that status instead of a 500.
- Not done, on purpose: no CSP (the pages use inline scripts and the GSAP CDN; a strict policy would need a rework), no 2FA, no client-side reschedule (cancel + rebook under the rules covers it), no image recompression (the largest is 190 KB).
- Tests: smoke +20 (429), e2e +6 (119), admin-e2e +5 (93).

## Captains: a therapist who trains another earns a share of what they make (2026-09-14, Ash)

Ash: "if they train another therapist, they get ten percent of their margin… once we train someone and they go back to their home place, they can open their profile on our website and start providing their services. The team captain makes ten percent out of them, and I make ten percent of that ten percent."

- **The tree.** `therapists.captain_id` says who trained whom (migration 029, applied to production). One level only: a captain earns on the people they trained, not on the people those people train (Ash didn't ask for a pyramid; easy to add later if he wants it).
- **The invite link.** Every therapist with a sign-in has **Team → Your team** in the admin: their personal link (`zenrecovery.club/join?ref=<code>`, code made on first open, e.g. `hesham-7k2p`), a copy button, the people on their team and the applications still waiting for the owner. The link opens the join page with a banner ("You're applying to join Hesham's team…") and the city preset; the application stores `captain_id`; the captain gets an email that someone applied through their link, the owner's email says who trained them; on approval the new profile is linked to the captain. The owner can also set or change **Trained by** in any therapist's dialog (no self, no loops; owner only). The public team page shows "Trained by …".
- **The money** (in `statements()`, shown in the Money tab and in the monthly email): for a trained therapist, `captain_due = captain_pct` (setting, default 10, owner edits it under Settings) of what they made that month, site takings after the platform fee **plus cash**; the platform keeps `PLATFORM_ON_CAPTAIN_BPS` (10%) of that share, the captain gets the rest. The trainee's line reads "To your captain, X (10%): − …"; the captain's line reads "From your team: + …" with each trainee's share (that is the one place a captain sees a number about a trainee: the share, not the sessions); the owner's and partners' city totals show "Captain shares inside that … of which … to Amico Mio"; the platform account sees its slice added to the 10%. Nothing moves money yet: like "to pay the team", these are the figures the owner settles; on cash sessions the trainee settles the share with the captain directly.
- **"Somewhere else".** Ash's larger idea is opening rooms wherever a trained therapist lives. The join form now has **Somewhere else…** with a free city field; such an application is stored with `city = 'other'` and the text, the owner's email says "someone asking to open it", and it **can't be approved until the city exists** (the admin says so; decline works). That captures the demand. Actually opening a new city is the next slice and needs Ash's decisions first: cities are fixed in `catalog.js` (currency, time zone, services, payment provider: Fawry only works in Egypt, Stripe in Italy), so "anywhere" means making cities data plus deciding how a new country gets paid.
- Tests: smoke +33 (462), e2e +5 (124), admin-e2e +6 (99).

## Ideas for later (not built — Ash decides)

Built on 2026-09-12 (first batch): login bar, Google sign-in, three languages, session rating + therapist note, weekly check-ins with a progress chart, add-to-calendar, reschedule via WhatsApp, exercises per focus area, installable app. Built the same evening (second batch): items 15–30 below, plus 1–4, 7–9 and 13 which they overlap with — see the table above. Still genuinely open: 5 (deposit model), 6 (fee on hand-added bookings — a business decision), 10 (body map in the guest booking form; signed-in clients already have it in the questionnaire), 12 (link-in-bio page). Kept here for the record:

1. **Live availability.** Today the client picks a day and a window and Zen confirms by WhatsApp. A real slot table per therapist (open hours per city, session length, buffers) would let clients pick an exact time and would stop double-bookings. Medium effort; the schema already has a slot column.
2. **WhatsApp reminders** the day before (and "how was it?" the day after) via the WhatsApp Business API or Twilio. Missed sessions are the biggest silent cost for a one-person room.
3. **Gift sessions.** A "buy a session for someone" flow: same checkout, produces a code. Recovery is an easy gift, and it brings in a new client every time.
4. **Packages.** 5 sessions at a discount, paid up front. Better cash flow for Zen, and the platform fee is taken once on the package.
5. **Deposit instead of full payment** for Egypt if Stripe stays Italy-only: card deposit in EUR, rest in cash. Already supported by `catalog.js`.
6. **Platform fee on hand-added bookings.** Sessions Zen enters manually carry no 2%. If most Egyptian sessions end up entered by hand, Locali & Ordinazioni's cut disappears. Options: a monthly invoice for 2% of manual sessions (the admin already counts them), or a flat monthly fee for the software instead of/alongside the 2%. Worth deciding with Zen before launch.
7. **Therapist accounts** (a third role) so each therapist sees only their own day, and clients can pick a therapist they liked.
8. **Before/after photos** in the client's profile, uploaded by the therapist with consent, so progress is visible over months. Needs R2 storage (small).
9. **Review request** email or WhatsApp after the 2nd session, linking to Google Maps / Instagram, with the invite link in the same message. Mirrors what the Tours concierge already does with Template C.
10. **Body map** in the booking form: tap where it hurts (back, shoulder, calf) instead of typing a note. Therapist sees it on the booking.
11. **Multi-language** (Arabic for Cairo/Dahab, Italian for Florence). The copy is short; a JSON of strings per language would do.
12. **Instagram link-in-bio page** at `/ig` with the three cities and "book now", so the profile link goes straight to booking.
13. **Corporate / gym partnerships**: a code per gym (Gold's Gym in the photos) giving members a fixed discount, with a per-gym report in the admin. Same mechanism as the invite codes.
14. **Head Chef digest line** for Zen: once live, the daily rundown can include "Zen: N bookings, X platform fee" from the admin stats endpoint, so Ash never has to open the admin to know how the 2% is doing.
15. **Apple sign-in** next to Google, mainly for iPhone users in Italy. Same server-side flow, one more client id.
16. **Session reminders by WhatsApp** (24 h before, and a "how do you feel?" nudge 2 days after) through the WhatsApp Business API. Biggest remaining lever against no-shows; needs Meta business verification.
17. **Live time slots** per therapist instead of morning/afternoon/evening, with the admin blocking off days. Removes the confirmation step on WhatsApp.
18. **Packages and memberships** (5 sessions up front, monthly plan) with the 2% taken once per package.
19. **Gift a session** with a code the recipient redeems in the booking form.
20. **Birthday reward**: automatic 20% credit in the client's birthday month (needs a scheduled Worker; birthdays are already stored).
21. **Before/after photos** in the client profile, uploaded by the therapist with consent (R2 storage).
22. **Waitlist**: "tell me if a slot opens on Friday" once live slots exist.
23. **Referral leaderboard** per city in the admin, and a small monthly prize for the top inviter.
24. **Gym partner codes** (Gold's Gym first) with a fixed member discount and a per-gym report.
25. **Export my data** for the client (PDF of sessions, check-ins and notes) to hand to a physio or doctor.
26. **Therapist profiles** on the site (photo, bio, languages spoken) and "book with the same therapist".
27. **SMS fallback** for sign-in links in Egypt, where email is checked less often than WhatsApp.
28. **Reviews to Google Maps**: after a 5-star rating in the app, one tap to paste it as a public review.
29. **Health-flag rules**: block online booking for flagged conditions until the therapist has approved the client once (today it's only a warning in the admin).
30. **Head Chef line**: daily digest of Zen bookings, ratings and the 2% fee from the stats endpoint.
